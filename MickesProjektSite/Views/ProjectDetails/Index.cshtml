@model IEnumerable<MickesProjektSite.Models.ProjectDetail>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Todo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Person.LastName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Project.ProjectTitle)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Todo)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Person.LastName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Project.ProjectTitle)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ProjectDetailsID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ProjectDetailsID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ProjectDetailsID })
        </td>
    </tr>
}

</table>
